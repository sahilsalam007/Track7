<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Failed - Vanguard</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    
    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
        }
        
        .order-status-card {
            max-width: 450px;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            padding: 30px;
            animation: fadeIn 0.5s ease-in-out;
        }
        
        .status-img {
            max-width: 180px;
            display: block;
            margin: 0 auto;
            animation: pop 0.6s ease-in-out;
        }
        
        .btn-primary {
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 10px 20px;
            font-weight: 500;
            border-radius: 8px;
        }
        
        .btn-primary:hover {
            background-color: #bb2d3b;
            color: white;
        }
        
        .btn-outline-secondary {
            border: 1px solid #dc3545;
            color: #dc3545;
            padding: 10px 20px;
            font-weight: 500;
            border-radius: 8px;
        }
        
        .btn-outline-secondary:hover {
            background-color: #dc3545;
            color: white;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes pop {
            0% {
                transform: scale(0.8);
                opacity: 0;
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }
        
        .text-red {
            color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="card order-status-card text-center p-4">
            <img src="https://cdn-icons-png.flaticon.com/512/458/458594.png" 
                 alt="Payment Failed" 
                 class="status-img mb-3"
                 style="width: 120px;">

            <h2 class="text-red mb-3">Payment Failed</h2>
            <p class="text-muted mb-4">We're unable to process your payment. Please check your payment details and try again.</p>

         
            <div class="mt-4">
                <a href="/failed-orders" class="btn btn-primary me-2">View Failed orders</a>
                <a href="/contact" class="btn btn-outline-secondary">Contact Support</a>
            </div>

            <div class="support-info mt-4">
                <p class="small text-muted mb-0">Need help? Email: support@vanguard.com</p>
                <p class="small text-muted">or call: 1-800-TRACK7</p>
            </div>
        </div>
    </div>
</body>
</html>